({
    //Java Script controller used for Lead Search
	search : function(component, event, helper) {
		helper.helperSearch(component, event);
	},
    
    returnLeads : function(component, event, helper){
        //var myTextMssg = event.getParam("myText");
		//component.set('v.myText', myTextMssg);
    }
})